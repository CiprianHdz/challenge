

Part 1. 

> Logistic Regression was chosen because is simpler and easier to interpret and diagnose.

Part II.

> For this part w/o modifying the tests files. We obtain 96% of total coverage. The missing pieces are because the payloads for some test are the same, thus they are not truly evaluated. Tbe modyfing verios of those are kept in another branch. 

